---
to: src/utils/<%= h.changeCase.camel(name) %>/test.ts
---
<%_ var nameCap = h.changeCase.camel(name);_%>
import { <%= nameCap %> } from "."

describe("<%= nameCap %>", () => {
  it("works", () => {
    // Arrange
    const val = 1

    // Act
    const received = <%= nameCap %>(val)

    // Assert
    expect(received).toEqual(1)
  })
})
